---
phase: 04-player-list-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routes/division/players.tsx
  - app/components/player-list.tsx
  - app/data/data.ts
autonomous: true
requirements:
  - BROWSE-01
  - BROWSE-02
must_haves:
  truths:
    - "Division member views a list of all players showing key identity fields"
    - "List is sortable by any column"
    - "List is filterable by position and club"
    - "List is searchable by player name"
  artifacts:
    - path: "app/routes/division/players.tsx"
      provides: "Player list route with loader"
      exports: ["loader", "default"]
    - path: "app/components/player-list.tsx"
      provides: "Player list UI component"
      contains: "PlayerList, PlayerRow"
    - path: "app/data/data.ts"
      provides: "getPlayers data function"
      exports: ["getPlayers"]
  key_links:
    - from: "app/routes/division/players.tsx"
      to: "app/components/player-list.tsx"
      via: "component import"
      pattern: "import.*PlayerList"
    - from: "app/routes/division/players.tsx"
      to: "app/data/data.ts"
      via: "loader data fetch"
      pattern: "getPlayers\\(\\)"
---

<objective>
Create the player list route and basic table component with data fetching.

Purpose: Establish the foundation for Phase 4 by creating the route, fetching player data, and rendering a basic sortable table.

Output: Working player list at `/division/players` with all players displayed in a table, sortable columns.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-player-list-search/04-UI-SPEC.md
@app/components/reports-table.tsx
@app/data/data.ts
@app/data/types.ts
</context>

<tasks>
<task type="auto">
<name>task 1: Create player list data fetching</name>
<files>app/data/data.ts</files>
<action>
The getPlayers function already exists and returns all players. No modification needed — this task is to verify the function is exported and usable.

Verify: `getPlayers()` function exists in app/data/data.ts and returns Promise<Player[]>
</action>
<verify>
<automated>grep -n "export async function getPlayers" app/data/data.ts</automated>
</verify>
<done>getPlayers function confirmed exported and available for import</done>
</task>

<task type="auto">
<name>task 2: Create PlayerList component</name>
<files>app/components/player-list.tsx</files>
<action>
Create a new component `PlayerList` that renders a table with the following columns:
- Player (name, clickable link to profile)
- Position (show position, use Badge component)
- Club
- Age (calculated from dateOfBirth)
- Reports (count of reports for this player)
- Last Scouted (most recent report date)

Component receives:
- players: Player[]
- sortBy: string
- sortDirection: 'asc' | 'desc'
- onSort: (column: string) => void

The component should:
1. Sort players based on sortBy and sortDirection props
2. Display column headers that are clickable to toggle sort
3. Show sort indicators (↑/↓) on active sort column
4. Handle empty state with "No players yet" message

Use the same table pattern as reports-table.tsx but adapted for player data.
</action>
<verify>
<automated>Test-Path app/components/player-list.tsx</automated>
</verify>
<done>PlayerList component created with sortable table, 6 columns, empty state handling</done>
</task>

<task type="auto">
<name>task 3: Create /division/players route</name>
<files>app/routes/division/players.tsx</files>
<action>
Create new route file at app/routes/division/players.tsx:
1. Create directory app/routes/division if it doesn't exist
2. Export a loader function that calls getPlayers()
3. Export default component that renders PlayerList with data from loader
4. Use useLoaderData hook to access player data
5. Implement basic sort state with useState (default sort by createdAt desc)

Pattern to follow: app/routes/scout/reports.tsx
</action>
<verify>
<automated>Test-Path app/routes/division/players.tsx</automated>
</verify>
<done>Route created at /division/players, loads players, renders PlayerList</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server | Player data fetched via loader, no user input in GET request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Player list data | accept | Read-only display of player data from trusted server source, no client-side mutation |
| T-04-02 | Information Disclosure | Player PII | accept | Only displays scouting-relevant data (name, position, club, DOB), no sensitive PII exposed |
</threat_model>

<verification>
1. Navigate to `/division/players` — table displays all players
2. Click column headers — sort toggles ascending → descending → no sort
3. Verify default sort is by creation date (newest first)
4. Verify empty state shows when no players exist
</verification>

<success_criteria>
- [ ] Player list route exists at `/division/players`
- [ ] Table shows 6 columns: Player, Position, Club, Age, Reports, Last Scouted
- [ ] All players from data layer are displayed
- [ ] Column headers are clickable and toggle sort direction
- [ ] Sort indicator (↑/↓) shows on active sort column
- [ ] Empty state displays when no players exist
</success_criteria>

<output>
After completion, create `.planning/phases/04-player-list-search/04-01-SUMMARY.md`
</output>
