---
phase: 04-player-list-search
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/components/player-list.tsx
  - app/routes/division/players.tsx
autonomous: true
requirements:
  - BROWSE-01
  - BROWSE-02
must_haves:
  truths:
    - "Division member can search players by name"
    - "Division member can filter by position"
    - "Division member can filter by club"
    - "Search and filters work together with AND logic"
  artifacts:
    - path: "app/components/player-list.tsx"
      provides: "Search input, position filter, club filter"
      contains: "SearchInput, PositionFilter, ClubFilter"
    - path: "app/routes/division/players.tsx"
      provides: "Search/filter state management"
      exports: ["default"]
  key_links:
    - from: "app/routes/division/players.tsx"
      to: "app/components/player-list.tsx"
      via: "search/filter props"
      pattern: "search=.*filter="
---

<objective>
Add search by name and filter by position/club functionality to the player list.

Purpose: Enable division members to find players of interest quickly through search and filters.

Output: Player list with working search bar and position/club dropdown filters.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-player-list-search/04-UI-SPEC.md
@app/components/player-list.tsx
@app/routes/division/players.tsx
</context>

<tasks>
<task type="auto">
<name>task 1: Add search input component</name>
<files>app/components/player-list.tsx</files>
<action>
Add a search input field above the table:
1. Use shadcn Input component
2. Placeholder: "Search by player name..."
3. Debounce not needed — filter on every keystroke (instant feedback)
4. Pass search value to parent via onSearch callback
5. Include Search icon (Lucide React)

Component receives:
- search: string
- onSearch: (value: string) => void
</action>
<verify>
<automated>grep -n "Search" app/components/player-list.tsx</automated>
</verify>
<done>Search input added with correct placeholder and icon</done>
</task>

<task type="auto">
<name>task 2: Add position and club filter dropdowns</name>
<files>app/components/player-list.tsx</files>
<action>
Add two filter dropdowns next to search:
1. Position filter:
   - Options: All positions, GK, DEF, MID, FWD (from PositionGroup enum)
   - Use shadcn Select component
   - Default: "All positions"

2. Club filter:
   - Options: All clubs + unique clubs from player list
   - Use shadcn Select component
   - Default: "All clubs"
   - Extract unique clubs from players array

Component receives:
- positionFilter: string
- clubFilter: string
- onPositionFilterChange: (value: string) => void
- onClubFilterChange: (value: string) => void
</action>
<verify>
<automated>grep -n "Position\|Club" app/components/player-list.tsx</automated>
</verify>
<done>Position and club filter dropdowns added with correct options</done>
</task>

<task type="auto">
<name>task 3: Implement search and filter logic in route</name>
<files>app/routes/division/players.tsx</files>
<action>
In the route component:
1. Add state for search, positionFilter, clubFilter using useState
2. Filter players based on all three criteria (AND logic):
   - Search: player.name.toLowerCase().includes(search.toLowerCase())
   - Position: positionFilter === 'all' OR player.positionGroup === positionFilter
   - Club: clubFilter === 'all' OR player.club === clubFilter
3. Pass filtered players and callbacks to PlayerList
4. Preserve sort state from Plan 01
5. Store filter state in URL search params for bookmarkability (use useSearchParams)

URL params format: ?search=...&position=...&club=...&sortBy=...&sortDir=...
</action>
<verify>
<automated>grep -n "useSearchParams\|filter\|search" app/routes/division/players.tsx</automated>
</verify>
<done>Search and filter logic implemented with URL state preservation</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server | Search/filter params sent via URL, no sensitive data exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing | URL parameters | accept | URL params are user-controlled, no authentication context leaked |
| T-04-04 | Tampering | Filter state | accept | Client-side filtering only, no server mutation possible |
</threat_model>

<verification>
1. Type in search box — player list filters by name instantly
2. Select position filter — list shows only players in that position group
3. Select club filter — list shows only players from that club
4. Combine search + filters — all criteria apply with AND logic
5. Refresh page — search/filter state persists in URL
</verification>

<success_criteria>
- [ ] Search input filters players by name (case-insensitive substring match)
- [ ] Position dropdown filters by position group (GK/DEF/MID/FWD)
- [ ] Club dropdown filters by club name
- [ ] Multiple filters combine with AND logic
- [ ] Filter state persists in URL search params
- [ ] Empty state shows when no players match filters
</success_criteria>

<output>
After completion, create `.planning/phases/04-player-list-search/04-02-SUMMARY.md`
</output>
