---
phase: 03-draft-report-management
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - app/routes/scout/reports.tsx
  - app/components/reports-table.tsx
  - app/routes.ts
  - app/lib/scoring/average.ts
autonomous: true
requirements:
  - SCOUT-04
must_haves:
  truths:
    - "Scout views list of their submitted reports"
    - "Reports table shows player, date, opponent, competition, result, average"
    - "Scout dropdown filters reports"
    - "Overall average excludes null ratings from denominator"
  artifacts:
    - path: "app/routes/scout/reports.tsx"
      provides: "My reports page with loader and component"
      exports:
        - "loader"
        - "component"
    - path: "app/components/reports-table.tsx"
      provides: "Table component for reports display"
      exports:
        - "ReportsTable"
    - path: "app/lib/scoring/average.ts"
      provides: "Overall average calculation"
      exports:
        - "calculateOverallAverage"
  key_links:
    - from: "app/routes/scout/reports.tsx"
      to: "app/data/data.ts"
      via: "getReportsByScout import"
      pattern: "getReportsByScout"
    - from: "app/components/reports-table.tsx"
      to: "app/lib/scoring/average.ts"
      via: "calculateOverallAverage import"
      pattern: "calculateOverallAverage"
---

<objective>
Create "My Reports" table view for scouts to review their submitted reports

Purpose: Enable scouts to view their report history (SCOUT-04) with filtering, sortable columns, and overall average calculation

Output: Reports route, table component, average calculation utility
</objective>

<execution_context>
@C:\Users\agelo\React\FootScout\footscout\.planning\get-shit-done\workflows\execute-plan.md
@C:\Users\agelo\React\FootScout\footscout\.planning\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-draft-report-management/03-CONTEXT.md
@.planning/phases/03-draft-report-management/03-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@app/routes/scout/report.tsx
@app/data/data.ts
@app/data/types.ts
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->
From app/data/types.ts:
```typescript
export type Report = {
  id: string;
  playerId: string;
  scoutId: string;
  matchDate: string;
  opponent: string;
  competition: string;
  scoutName: string;
  physical: { /* attributes */ };
  technical: { /* attributes */ };
  tactical: { /* attributes */ };
  matchNotes: { notes: string };
  status: 'draft' | 'submitted';
  currentStep: number;
  createdAt: string;
  updatedAt: string;
};

export type Player = {
  id: string;
  name: string;
  dateOfBirth: string;
  position: string;
  club: string;
  nationality: string;
};
```

From app/data/data.ts (Plan 01):
```typescript
export function getReportsByScout(scoutId: string, status?: 'draft' | 'submitted'): Promise<Report[]>;
export function getPlayers(): Promise<Player[]>;
```
</interfaces>

<tasks>
<task type="auto">
<name>task 1: Create overall average calculation utility</name>
<files>app/lib/scoring/average.ts</files>
<action>
1. Create app/lib/scoring/average.ts:

```typescript
import type { Report } from "../data/types";

/**
 * Calculate overall average for a report
 * Excludes null ratings from denominator (D-11, D-12)
 * Only includes physical, technical, tactical attributes (not matchNotes)
 */
export function calculateOverallAverage(report: Report): number {
  const attributeValues: number[] = [];
  
  // Physical attributes
  if (report.physical) {
    Object.values(report.physical).forEach((value) => {
      if (value !== null && value !== undefined) {
        attributeValues.push(value);
      }
    });
  }
  
  // Technical attributes
  if (report.technical) {
    Object.values(report.technical).forEach((value) => {
      if (value !== null && value !== undefined) {
        attributeValues.push(value);
      }
    });
  }
  
  // Tactical attributes
  if (report.tactical) {
    Object.values(report.tactical).forEach((value) => {
      if (value !== null && value !== undefined) {
        attributeValues.push(value);
      }
    });
  }
  
  if (attributeValues.length === 0) {
    return 0;
  }
  
  const sum = attributeValues.reduce((acc, val) => acc + val, 0);
  return sum / attributeValues.length;
}

/**
 * Calculate overall average with formatting
 * Returns string with 2 decimal places
 */
export function formatOverallAverage(report: Report): string {
  const avg = calculateOverallAverage(report);
  return avg.toFixed(2);
}
```

2. Export functions
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<done>
- app/lib/scoring/average.ts exists
- calculateOverallAverage function excludes null values
- calculateOverallAverage only includes physical, technical, tactical (not matchNotes)
- formatOverallAverage returns string with 2 decimals
- TypeScript compilation passes
</done>
</task>

<task type="auto">
<name>task 2: Create reports table component</name>
<files>app/components/reports-table.tsx</files>
<action>
1. Create app/components/reports-table.tsx:

```typescript
import type { Report, Player } from "../data/types";
import { calculateOverallAverage } from "../lib/scoring/average";

interface ReportsTableProps {
  reports: Report[];
  players: Player[];
}

export function ReportsTable({ reports, players }: ReportsTableProps) {
  if (reports.length === 0) {
    return (
      <div className="text-center py-12">
        <p className="text-gray-500">No reports found</p>
      </div>
    );
  }

  const getPlayerName = (playerId: string) => {
    const player = players.find((p) => p.id === playerId);
    return player?.name || "Unknown Player";
  };

  return (
    <div className="overflow-x-auto">
      <table className="min-w-full divide-y divide-gray-200">
        <thead className="bg-gray-50">
          <tr>
            <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Player
            </th>
            <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Match Date
            </th>
            <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Opponent
            </th>
            <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Competition
            </th>
            <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Result
            </th>
            <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Avg Score
            </th>
          </tr>
        </thead>
        <tbody className="bg-white divide-y divide-gray-200">
          {reports.map((report) => (
            <tr key={report.id} className="hover:bg-gray-50">
              <td className="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                {getPlayerName(report.playerId)}
              </td>
              <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                {new Date(report.matchDate).toLocaleDateString()}
              </td>
              <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                {report.opponent}
              </td>
              <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                {report.competition}
              </td>
              <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                {report.matchNotes.result || "-"}
              </td>
              <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900 font-semibold">
                {calculateOverallAverage(report).toFixed(2)}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

2. Export component
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<done>
- ReportsTable component exists
- Table shows 6 columns: Player, Date, Opponent, Competition, Result, Avg Score
- Overall average calculated per report (nulls excluded)
- Empty state shown when no reports
- TypeScript compilation passes
</done>
</task>

<task type="auto">
<name>task 3: Create reports route and wire everything together</name>
<files>app/routes/scout/reports.tsx, app/routes.ts</files>
<action>
1. Create app/routes/scout/reports.tsx:

```typescript
import { json } from "react-router";
import type { LoaderFunctionArgs } from "react-router";
import { getScoutIdFromCookie } from "../../cookies.server";
import { getReportsByScout, getPlayers } from "../../data/data";
import { ReportsTable } from "../../components/reports-table";

export async function loader({ request }: LoaderFunctionArgs) {
  const scoutId = await getScoutIdFromCookie(request);
  const url = new URL(request.url);
  const filterScoutId = url.searchParams.get("scoutId") || scoutId;
  
  // Get submitted reports (exclude drafts)
  const reports = filterScoutId
    ? await getReportsByScout(filterScoutId, "submitted")
    : [];
  const players = await getPlayers();
  const scouts = await getScouts();

  return json({
    reports,
    players,
    scouts,
    scoutId: filterScoutId,
  });
}

export default function ReportsPage() {
  const { reports, players, scouts, scoutId } = useLoaderData<typeof loader>();

  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <h1 className="text-2xl font-bold text-gray-900 mb-6">My Reports</h1>
      
      {/* Scout dropdown filter (D-10) */}
      <div className="mb-6">
        <label htmlFor="scout-filter" className="block text-sm font-medium text-gray-700 mb-2">
          Filter by Scout
        </label>
        <select
          id="scout-filter"
          defaultValue={scoutId || ""}
          onChange={(e) => {
            const url = new URL(window.location.href);
            if (e.target.value) {
              url.searchParams.set("scoutId", e.target.value);
            } else {
              url.searchParams.delete("scoutId");
            }
            window.location.href = url.toString();
          }}
          className="block w-full max-w-xs rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm"
        >
          <option value="">All Scouts</option>
          {scouts.map((scout) => (
            <option key={scout.id} value={scout.id}>
              {scout.name}
            </option>
          ))}
        </select>
      </div>

      <ReportsTable reports={reports} players={players} />
    </div>
  );
}
```

2. Update app/routes.ts to add the new route:
```typescript
import { route } from "react-router";

export default [
  // ... existing routes
  prefix("scout", [
    // ... existing scout routes
    route("reports", "routes/scout/reports.tsx"),
  ]),
];
```

3. Ensure proper imports and types
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<done>
- app/routes/scout/reports.tsx exists with loader and component
- Loader fetches reports filtered by scout and status
- Scout dropdown filter present and functional
- ReportsTable component used
- app/routes.ts includes /scout/reports route
- TypeScript compilation passes
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client→Server (URL params) | Untrusted input from URL search params |
| Server→File System | Trusted (internal data operations) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | URL scoutId param | mitigate | Validate scoutId against scouts list in loader |
| T-03-08 | Information Disclosure | reports data | accept | Scout-only view; reports filtered by scoutId |
| T-03-09 | Improper Authorization | cross-scout access | mitigate | Server-side filter ensures scouts only see their own reports |
</threat_model>

<verification>
[Phase 3 Plan 3 verification]
1. TypeScript compilation passes: `npm run typecheck`
2. app/lib/scoring/average.ts exports calculateOverallAverage
3. calculateOverallAverage excludes null values from denominator
4. ReportsTable component shows 6 columns
5. app/routes/scout/reports.tsx has loader with scout filter
6. app/routes.ts includes /scout/reports route
7. Scout dropdown filters reports by scoutId
</verification>

<success_criteria>
Reports list operational:
- Scouts see table of their submitted reports
- Table shows player, date, opponent, competition, result, average
- Scout dropdown filters reports
- Overall average correctly excludes nulls
- Route accessible at /scout/reports
</success_criteria>

<output>
After completion, create `.planning/phases/03-draft-report-management/03-03-SUMMARY.md`
</output>
